//allOutfits should be an array of Objects
//tags needs to be an array
//potentially needs a sql query because the function that returns the array of all objects returns html components to render on the page
function filterByTags(allOutfits, tags){
    
}